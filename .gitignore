# Previously I have copied-and-pasted from here
# https://github.com/github/gitignore/blob/master/Rails.gitignore
# This time I'm adding items one at a time as they come up

ignore/*
# config/secrets.yml # Trying to debug this Heroku error message
# that I see on https://gothenburger.herokuapp.com/ right now:
# "Missing `secret_token` and `secret_key_base` for 'production' environment, set these values in `config/secrets.yml`"
log/
tmp/
.env
TODO.md
